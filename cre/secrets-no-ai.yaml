# CRE Secrets — for workflows that don't need AI keys.
# Export before running simulate:
#   export BACKEND_URL_VALUE="http://localhost:3001/api"

secretsNames:
    BACKEND_URL:
        - BACKEND_URL_VALUE
